export class Page {
    width: number;
    height: number;
    url: string;

    constructor(width: number, height: number, url: string) {
        this.width = width;
        this.height = height;
        this.url = url;
    }

}
